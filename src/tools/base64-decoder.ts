/**
 * Base64 decoder tool for the Space Station Escape Challenge
 *
 * Users need to implement this function to decode the initial emergency message.
 * The message contains the encryption key needed for the next puzzle.
 *
 * Hint: Node.js has built-in Buffer support for base64 operations.
 */

export async function decodeBase64(encodedText: string): Promise<string> {
  // TODO: Implement base64 decoding
  // Hint: Use Buffer.from(encodedText, 'base64').toString('utf8')

  throw new Error("🚨 Base64 decoder not implemented - decrypt the initial emergency message to find the encryption key!");
}